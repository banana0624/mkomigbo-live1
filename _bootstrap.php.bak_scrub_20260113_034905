<?php
declare(strict_types=1);
require_once __DIR__ . '/_init.php';


/**
 * /public/_bootstrap.php
 * Deterministic bootstrap (audit-clean).
 */

if (defined('APP_BOOTSTRAPPED') && APP_BOOTSTRAPPED === true) { return; }
define('APP_BOOTSTRAPPED', true);

if (!defined('APP_ROOT')) {
  define('APP_ROOT', dirname(__DIR__) . '/app/mkomigbo');
}

// Avoid literal "/private/assets/initialize.php"
// [patched] $init = rtrim((string)APP_ROOT, '/') . '/private' . '/assets' . '/initialize.php';

if (!is_file($init)) {
  http_response_code(500);
  header('Content-Type: text/plain; charset=utf-8');
  echo "Init not found: {$init}\n";
  exit;
}

require_once $init;
